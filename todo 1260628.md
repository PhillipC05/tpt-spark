# TPT Spark — TODO

## Engine: Quantization

- [ ] Wire up all quantization formats via dtype-aware dequantization dispatch
  - Q4_K (shader exists, just needs wiring)
  - Q4_0, Q5_0, Q5_1 — simple block formats, new shaders
  - Q2_K, Q3_K, Q5_K, Q6_K — K-quant formats, new shaders
  - F16 → F32 conversion shader
  - F32 passthrough (copy, no shader)

## Engine: Architecture & Correctness

- [ ] Add fused QKV support (`attn_qkv.weight` — Phi-3, MiMo, some Qwen3)
- [ ] Add missing architectures: starcoder2, phi2, solar, baichuan, baichuan2, grok, falcon
- [ ] Read RMSNorm epsilon from GGUF metadata (currently hardcoded 1e-5)
- [ ] Support attention bias terms (`attn_q.bias`, `attn_output.bias`, etc.)
- [ ] Read RoPE scaling metadata (YaRN / NTK for extended context)

## Engine: Features

- [ ] Multi-turn KV cache (skip re-running already-cached tokens on follow-up messages)

## Frontend

- [ ] Show model load progress (currently no feedback while uploading weights to VRAM)
- [ ] Conversation history / multi-turn UI (pairs with multi-turn KV cache)
